# Scoped `hxd.Res` smoke tests

Run from repository root:

- Local file system mode:
  - `haxe tests/scoped_res/local.hxml`
- Embedded resources mode:
  - `haxe tests/scoped_res/embed.hxml`
- Pak mode:
  - `haxe tests/scoped_res/build_paks.hxml`
  - `haxe tests/scoped_res/pak.hxml`

Expected output contains `ok`.
